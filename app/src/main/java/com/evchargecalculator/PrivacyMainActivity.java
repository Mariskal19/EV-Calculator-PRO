package com.evchargecalculator;

/**
 * Launcher activity. The privacy-policy link is part of MainActivity itself so it remains visible
 * after rebuilding the UI when the theme is changed.
 */
public class PrivacyMainActivity extends MainActivity {}
