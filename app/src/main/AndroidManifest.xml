<manifest xmlns:android="http://schemas.android.com/apk/res/android">
    <uses-permission android:name="android.permission.INTERNET" />
    <application
        android:name=".AppMenuApplication"
        android:theme="@style/Theme.EVChargeCalculator"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:supportsRtl="true">
        <activity
            android:name=".PrincipalActivity"
           
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".PersistentMainActivity" android:exported="false" />
        <activity android:name=".ElectricVsCombustionActivity" android:exported="false" />
        <activity android:name=".CompararCochesActivity" android:exported="false" />
        <activity android:name=".PrivacyMainActivity" android:exported="false" />
        <activity android:name=".ConfigurationActivity" android:exported="false" />
    </application>
</manifest>
